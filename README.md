DSA Tracker – Backend
Node.js + Express + MongoDB backend for DSA Tracker.

Features
Auth with OTP & JWT

DSA questions CRUD

Revision tracking

User profile & platform stats

Setup
npm install
npm run dev
Runs on http://localhost:3000

Env
MONGODB_URI=
JWT_SECRET=
EMAIL_USER=
EMAIL_PASSWORD=
